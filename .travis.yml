language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t wmmk/docker-react -f Dockerfile.dev .

script:
  - docker run wmmk/docker-react npm run test -- --coverage


